<!DOCTYPE hyml>
<html>
    <head>
        <meta charset="utf-8" />
        <title> fananf - vos transcriptions </title>
        <link rel="stylesheet" href="/style/general.css">
		<link rel="stylesheet" href="/style/menu.css">	
    </head>
    <body>
        <%- include('partials/headerConnected') %>
        <h2>Liste des transcriptions auxquelles vous avez accès</h2>
        <tr>
            <td>
                Nom
            </td>
            <td>
                Statut
            </td>
            <td>
                Options
            </td>
        </tr> </br>
        <% transcriptions.forEach(function(transcription,index) { %>
            <tr>
                <td>
                    <%= transcription.name %> </a>
                </td>
                    <%= transcription.status%>
                <% if (transcription.status == "Done") { %>
                    <td>
                        <a href="/transcription/<%=transcription._id%>"> Consulter </a>
                    </td>
                <% } %>
            </tr>
        </br>
        <% }) %>
        <form method="GET" action="/transaction/<%=transaction._id%>/edit">
            <input type="submit" value="Modifier"/>
        </form>

    </body>
    <%- include('partials/footer') %>
</html>