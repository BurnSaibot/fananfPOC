<!DOCTYPE hyml>
<html>
    <head>
        <meta charset="utf-8" />
        <title> transcription : <%=transcription.name%> </title>
        <link rel="stylesheet" href="/stylesheet/general.css">
        <link rel="stylesheet" href="/stylesheet/menu.css">		
    </head>
    <body>
        <%- include('partials/headerConnected') %>
        <h2>Liste des sous-titres disponibles la transcription : <%= transcription.name %></h2>
        <tr>
            <td>
                Nom
            </td>
            <td>
                Options
            </td>
        </tr>
    </br>
        <% subtitles.forEach(function(sub,index) { %>
            <tr>
                <td>
                    <%= transcription.name %>.<%=sub.format%> </a>
                </td>
                <td>
                    <a href="/subtitle/<%= sub._id %>"> Consulter </a>
                    <a href="/subtitle/edit/<%= sub._id %>"> Editer </a>
                </td>
            </tr>
        </br>
        <% }) %>

        <form method="GET" action="/transcription/<%=transcription._id%>/edit">
            <input type="submit" value="Modifier"/>
        </form>

    </body>
    <%- include('partials/footer') %>
</html>
